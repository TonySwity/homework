{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run active file (C/C++)",
			"type": "lldb",
			"request": "launch",
			"program": "${fileDirname}/${fileBasenameNoExtension}",
			"cwd": "${fileDirname}",
			"preLaunchTask": "build active file (auto gcc/g++)"
		}
	]
}
